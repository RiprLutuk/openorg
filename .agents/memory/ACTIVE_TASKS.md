# Active Tasks & Roadmap Tracker

---

## 🚀 Active Sprint / Tasks
- [x] Configure agent superpowers, UI/UX design skill, and project memory system.
- [x] Pivot architecture from dynamic CMS Builder to Standard Modular Web App.
- [x] Refactor `apps/web` into a clean Next.js application with native JSX pages (`/`, `/events`, `/structure`, `/verify`, `/join`, `/member`).
- [x] Verify build, typecheck, linting, and unit tests (45 tests passing).

---

## 📌 Backlog / Feature Pipeline
- [ ] Expand member self-service features in portal (`/member`).
- [ ] Add PDF certificate generation for completed Academy SKP activities.
